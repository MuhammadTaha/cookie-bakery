## Cookie Bakery Frontend

This project is developed using ReactJs.

* To run the project first run `npm install` to install all the dependencies. 
* Next, run `npm start` to run the application.

Enjoy your cookie.
